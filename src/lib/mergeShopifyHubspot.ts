// TODO (Phase 2): Match Shopify customer emails against HubSpot contacts.
// This will build the rep-owned vs webshop-only segmentation layer.
// Requires SHOPIFY_INTEGRATION_READY=true and populated /api/shopify/orders.
export async function mergeShopifyHubspot(): Promise<never[]> {
  return [];
}
